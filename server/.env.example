# Server Configuration
# Defines the port the application will run on and the environment mode.
PORT=
NODE_ENV=development

# JSON Web Token
# A long, random, and secret string used for signing authentication tokens.
JWT_SECRET=

# Email Configuration (using Gmail SMTP)
# These credentials are used for sending emails from your application.
# For EMAIL_PASS, generate an "App Password" from your Google Account security settings.
EMAIL_HOST=smtp.gmail.com
EMAIL_USER=
EMAIL_PASS=
EMAIL_PORT=

# Cloudinary Credentials
# Keys for the Cloudinary service to handle image/media uploads.
# You can get these from your Cloudinary dashboard.
CLOUDINARY_CLOUD_NAME=1
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Database Connection
# The connection URI for your PostgreSQL database.
DB_URI=